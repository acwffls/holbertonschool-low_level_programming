#include "main.h"
#include <stdio.h>

/**
 * add - look the description
 * Description: write a function that adds two integers and returns the result
 * @a: an integer
 * @b: an integer
 * Return: Result
 */
int add(int a, int b)
{
	return (a + b);
}
