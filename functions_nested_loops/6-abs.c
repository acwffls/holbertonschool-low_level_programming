#include <stdlib.h>
#include "main.h"

/**
 * _abs - look the description
 * Description: compute the absolute value of an integer
 * @n: an integer
 * Return: n or -n
 */

int _abs(int n)
{
	if (n < 0)
{
	return (-n);
}
	else
{
	return (n);
}
}
